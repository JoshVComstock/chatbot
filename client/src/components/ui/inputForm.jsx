export const InputForm = ({ children }) => (
  <div
    style={{
      display: "flex",
      alignItems: "center",
      backgroundColor: "#F5F5F5",
      borderRadius: "24px",
      padding: "6px",
      position: "relative",
    }}
  >
    {children}
  </div>
);
